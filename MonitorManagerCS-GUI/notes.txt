Moving all colors to system colors through ModernWPF so that themeing is easy

Improvements:
Make automated checkbox more intuitive

Add placeholder text for the VCP code combobox rather than preselecting brightness

Make the overlay for the chart (when inactive) communicate its purpose

Add some feedback when you save settings
	And maybe also for save&apply button for display settings

Zoom in on chart for accurate placement

Adjust snapping

Checkbox to show only automated VCP codes for each display

Copy VCP automation points and paste onto another VCP chart

Allow VCP codes to be 'linked' so that they are set in parallel and have the same values.

Dark Mode/Light Mode
	Also a setting to link to Windows' Dark/Light mode toggle.

Major Improvements:
Replace ControlMyMonitor by creating a independent DDC/CI interface